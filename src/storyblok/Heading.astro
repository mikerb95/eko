---
import Heading from '../components/Heading.astro'

const { blok } = Astro.props

const title = blok.title || 'Información'
const icon = blok.icon || null
const variant = blok.style || 'default'
---

<Heading title={title} icon={icon} variant={variant} />
